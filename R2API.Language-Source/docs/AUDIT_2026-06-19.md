# PELE plugin audit - 2026-06-19

Escopo: `R2API.Language-Source`, assets PELE necessarios e artefato `_build`.

## Resultado

Status: aprovado para backup/push.

## Verificacoes executadas

- `dotnet build R2API.Language.csproj -c Release -v:minimal`
  - resultado: sucesso, 0 erros.
- `git diff --check`
  - resultado: sem erros de whitespace.
- Scan de arquivos locais indesejados:
  - `bin/` e `obj/` estao ignorados.
  - `.zip`, `.log`, `.env`, temporarios e pastas de upload estao ignorados.
- Scan de segredos em texto:
  - nenhum segredo real encontrado.
  - hits existentes sao tokens de traducao, licenca ou conteudo de arquivos de linguagem.
- DLL instalada e `_build/R2API.Language.dll` sincronizadas:
  - tamanho: `227840`
  - hash SHA256 inicial: `54AA9F193982`

## Mudancas revisadas

- `LanguagePlugin.cs`
  - removida responsabilidade de fonte e parsing JSON.
  - mantido boot/lifecycle, registro da pasta PELE e hooks principais.
- `CyrillicFontSupport.cs`
  - isola patches de fonte e fallback de cirilico.
  - prioridade: `PELE/Fonts/cyrillicfont`, fallback externo se existir, fallback embutido antigo.
- `PeleJsonLoader.cs`
  - isola carregamento dos JSONs PELE.
  - suporta formato plano e objeto `strings`.
- `PeleStartupDiagnostics.cs`
  - loga uma vez os checks de startup.
  - verifica `PELE/Language`, `language.json`, `icon.png`, fonte cirilica e DLL duplicada.
- `LanguagePauseMenu.cs`
  - mantem UI do pause e legenda de input.
  - fechamento usa `dialog.rootObject` para evitar overlay escuro travado.

## Riscos residuais

- A validacao visual final ainda depende do jogo, porque layout de `SimpleDialogBox`
  varia com resolucao, idioma e fonte ativa.
- O proximo refactor recomendado e mover `FindLanguageByName` e
  `SetCurrentLanguage` para `CustomLanguageRegistration.cs`.
